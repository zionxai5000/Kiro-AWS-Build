{
  "name": "@seraphim/services",
  "version": "0.1.0",
  "private": true,
  "description": "System services: Mishmar, Zikaron, Otzar, XO Audit, Event Bus, Learning Engine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1040.0",
    "@aws-sdk/client-eventbridge": "^3.1040.0",
    "@aws-sdk/client-s3": "^3.1040.0",
    "@aws-sdk/client-sqs": "^3.1040.0",
    "@aws-sdk/lib-dynamodb": "^3.1040.0",
    "@seraphim/core": "*",
    "ajv": "^8.17.1",
    "dotenv": "^17.4.2",
    "playwright": "^1.60.0"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run --config ../../vitest.config.ts --project services",
    "lint": "eslint 'src/**/*.ts'",
    "clean": "tsc --build --clean && rm -rf dist"
  }
}
